# ─── Expo public variables (prefix with EXPO_PUBLIC_) ────────
# These are inlined at build time and visible in the client bundle.

# API base URL — use your machine's LAN IP for device testing
# e.g. http://192.168.1.x:3000
EXPO_PUBLIC_API_URL=http://192.168.0.179:3000
